{
  "name": "@impart-security/aws-log-forwarder",
  "version": "0.1.0",
  "description": "Lambda function to stream ec2 loadbalancer access logs and cloudwatch logs",
  "main": "index.js",
  "author": "Impart Security",
  "license": "Apache-2.0",
  "scripts": {
    "format": "prettier . --write --cache",
    "lint": "eslint . --cache --ext .js --max-warnings=0"
  },
  "dependencies": {
    "aws-sdk": "^2.1467.0"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "prettier": "^3.0.2"
  }
}
